# API
VITE_API_URL=http://localhost:8000
# false => the app talks to the real backend (required for the SIH demo).
# The automated frontend test suite forces VITE_USE_MOCK_API=true itself.
VITE_USE_MOCK_API=false